# Secrets — never commit (API keys live here)
.env

# Python
.venv/
venv/
__pycache__/
*.pyc

# Logs / runtime
*.log
env/
.env
env/*

# settings.json contains API keys — only agents.json belongs in git
data/settings.json
